
  # Dark-mode live streaming UI

  This is a code bundle for Dark-mode live streaming UI. The original project is available at https://www.figma.com/design/fKaECKI5fEAfWacOceZHLl/Dark-mode-live-streaming-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  